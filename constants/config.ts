// API Configuration
export const API_URL = 'https://backend-sga-production.up.railway.app/api';

// API Endpoints
export const ENDPOINTS = {
    LOGIN: `${API_URL}/auth/login`,
    CURSOS: `${API_URL}/cursos`,
    ESTUDIANTE: `${API_URL}/estudiante`,
};
